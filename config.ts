
// Change this to your deployed backend URL in production
// For local development, it usually defaults to http://localhost:5000
export const API_URL = process.env.REACT_APP_API_URL || 'http://localhost:5000/api';
